package com.solix.advanced.test;

public class Engine {
    
    public static native void print(char[] s);

    public static void main(string[] args) {
        
        int32 x = 10;
        int32 y = 20;
        int32 z = 30;

        x = y + z * 2 - 10 / 5;

        bool flag = false;
        bool result = true;

        if (x > 10) {
            x = x + 1;
        }

        Engine.print("Test SLX executed successfully!");
    }
}
